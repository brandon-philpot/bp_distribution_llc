import { Routes } from '@angular/router';

import { ServicesPageComponent } from '../services-page.component';

export const SERVICES_PAGE_ROUTE : Routes = [
  { path:'', component: ServicesPageComponent }
];
